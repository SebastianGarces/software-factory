{
  "name": "software-factory",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "factory": "bun run src/index.ts",
    "heartbeat": "bun run src/heartbeat.ts",
    "watch": "bun run src/monitor/watch.ts",
    "status": "bun run src/monitor/status.ts",
    "tail": "bun run src/monitor/tail.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
